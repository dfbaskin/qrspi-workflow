# Keep the working tree LF on every platform. The prompt files ship verbatim
# inside the release archives, so a CRLF checkout would hand users different
# bytes than a CI build produces.
* text=auto eol=lf

*.zip binary
